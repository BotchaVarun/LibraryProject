{
    "version": 2,
    "builds": [
      {
        "src": "./server.js", // Ensure this file exists
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "server.js" // Ensure this matches your entry point
      }
    ]
  }
  